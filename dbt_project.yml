
# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'my_new_project'
version: '1.0.0'
config-version: 2

# This setting configures which "profile" dbt uses for this project.
profile: 'default'

# These configurations specify where dbt should look for different types of files.
# The `source-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  # directory which will store compiled SQL files
clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models

# In this example config, we tell dbt to build all models in the example/ directory
# as tables. These settings can be overridden in the individual model files
# using the `{{ config(...) }}` macro.
models:
  my_new_project:
    # Applies to all files under models/example/
    example:
      materialized: view


#################################################################################################################
# name: 'my_new_project'
# version: '1.0.0'
# config-version: 2

# # Set the name of your GCP project and specify your desired profile.
# # You can create a profile in your profiles.yml file.
# profile: 'test-jiajun-project'

# # Set the paths for each type of file that dbt will look for.
# model-paths: ["models"]
# analysis-paths: ["analyses"]
# test-paths: ["tests"]
# seed-paths: ["seeds"]
# macro-paths: ["macros"]
# snapshot-paths: ["snapshots"]

# # Set the path for the compiled SQL code that dbt will output.
# target-path: "target"

# # Set the targets to be cleaned when running `dbt clean`.
# clean-targets:
#   - "target"
#   - "dbt_packages"

# # Define the models that dbt will build.
# models:
#   my_new_project:
#     horangi_all_data:
#       materialized: view
#       database: test-jiajun-project
#       schema: horangi_testing
